#!/usr/bin/env sh

DIR="$(dirname "$(readlink "$0")")"

GIT_PAGER="../utils/less -k ../utils/review-lesskey"
export GSARLANGA_PID=$$

trap "exit" SIGINT

for file in `git diff --name-only`; do
	export CURRENT_EDITING_FILE="$file"
	git diff "$file"
done

